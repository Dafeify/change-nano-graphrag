services:
  ship-inference:
    build:
      context: .
      dockerfile: Dockerfile
    image: ship-text-inference:baseline-v1
    container_name: ship-text-inference
    ports:
      - "8000:8000"
    env_file:
      - .env.docker
    restart: unless-stopped
